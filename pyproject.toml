[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "python-utils"
version = "1.0.1"
dependencies = [
    'torch',
    'numpy',
    "dicttoxml",
    "xmltodict",
]

[tool.black]
line-length = 120

[tool.isort]
profile = "black"
line_length = 120
